<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <Authors>Kjeld Ingemann Poulsen</Authors>
    <Company>Kipon ApS</Company>
    <Description>Library to stream .NET data to and from Excel (OpenXml)</Description>
    <Copyright>Kipon ApS 2019</Copyright>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
    <PackageProjectUrl>https://github.com/kip-dk/Kipon.Excel</PackageProjectUrl>
    <PackageIconUrl>https://s.gravatar.com/avatar/48d1270fbf870a670d20844eda19e3bf</PackageIconUrl>
    <RepositoryType>git</RepositoryType>
    <RepositoryUrl>https://github.com/kip-dk/Kipon.Excel</RepositoryUrl>
    <PackageTags>OpenXml Excel .NET Stream</PackageTags>
    <PackageReleaseNotes>Support for dictionary properties to allow dynamics columns.  Add the IndexColumn property to your sheet object definition, and define the colum as a Dictionary&lt;string,type&gt; where type is one of the simple types, also supported for cells.

Parseattribute allow you to start parse of a table in a specific row and specific column.  Add the [Parse] attribute to you Sheet class and set then row and column. The index starts at 1 as in excel.</PackageReleaseNotes>
    <Version>1.0.11-alpha</Version>
    <SignAssembly>true</SignAssembly>
    <AssemblyOriginatorKeyFile>Kipon.snk</AssemblyOriginatorKeyFile>
    <AssemblyVersion>1.0.11.0</AssemblyVersion>
    <FileVersion>1.0.11.0</FileVersion>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="DocumentFormat.OpenXml" Version="2.9.1" />
  </ItemGroup>

</Project>
